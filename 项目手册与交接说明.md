# SterneSite 个人博客 · 项目手册与 Agent 交接说明

- 创建日期：2026-08-08
- 适用对象：站长本人（日常操作备忘）＋ 后续接手的 AI Agent（工作交接）
- 项目位置：`E:\Desktop\SterneSite`

---

## 一、项目概览

- **性质**：纯静态个人博客，无后端、无构建步骤。所有文章数据写在 `js/main.js` 的 `posts` 数组里，由 JS 在浏览器端渲染。
- **部署链路**：本地改文件 → `git push` 到 GitHub → Cloudflare Pages 监听到新提交后 **1~2 分钟内自动重新部署**。中间无需任何手动操作。
- **GitHub 远程仓库**：`https://github.com/Watersterne/SterneSite.git`，分支 `main`。
- **本地 git 状态**：已初始化、已配置身份、已与远程关联，历史提交正常。

---

## 二、目录结构与文件职责

| 路径 | 职责 |
|---|---|
| `index.html` | 首页，只展示一张大图（`images/home.jpg`）＋图注 |
| `posts.html` | 文章列表页，含分类筛选（全部/技术/生活/读书） |
| `post.html` | 文章详情页，通过 URL 参数 `?id=N` 显示对应文章 |
| `about.html` | 关于页 |
| `css/style.css` | 全部样式：侧边栏布局、亮/暗主题（CSS 变量）、响应式（窄屏侧边栏折叠到顶部） |
| `js/main.js` | 文章数据 `posts` 数组 ＋ 列表/详情渲染 ＋ 筛选 ＋ 主题切换（localStorage 记忆） |
| `images/home.jpg` | 首页大图。用自己的同名文件覆盖即可换图，横版约 1600×900 效果最佳，建议 <2MB |
| `images/posts/` | Word 转文章时自动提取的正文配图 |
| `deploy.bat` | 一键部署：git add → commit（自动生成带日期的说明）→ push |
| `new_post.bat` / `new_post.py` | 一键新建文章骨架（交互输入标题/摘要/分类，id 自动递增，自动转义，写入后自检） |
| `doc2post.bat` / `doc2post.py` | Word 文档 → 博客文章转换器（依赖 `python-docx`） |
| `posts/` | Word 写文章的目录；转换成功的文档自动移入 `posts/processed/` 归档 |
| `posts/新文章模板.docx` | Word 写作模板（字体已修复，用这个） |
| `posts/模板-新文章.docx` | **旧模板，有字体缺陷，待用户关闭 Word 后手动删除** |
| `.gitignore` | 忽略 Thumbs.db / desktop.ini / .DS_Store |

四个 HTML 页面共用同一套侧边栏结构，导航为：首页 / 文章 / 关于，当前页高亮。

---

## 三、日常操作指南

### 3.1 发新文章（Word 方式，推荐）

1. 复制 `posts/新文章模板.docx`，改名（如 `2026-08-08-我的新文章.docx`）。
2. 在 Word 里按格式写：
   - **第 1 行**：标题（必填）
   - **第 2 行**：摘要（可留空）
   - **第 3 行**：分类，写"技术"/"生活"/"读书"（留空默认技术）
   - **第 4 行**：日期 `YYYY-MM-DD`（留空默认当天）
   - **第 5 行起**：正文
3. 正文样式约定：
   - Word"标题 1/2/3"样式 → 转成 `<h2>/<h3>/<h4>`
   - 代码用 **Consolas / Courier New** 等等宽字体（整段等宽 = 代码块 `<pre><code>`，行内等宽 = `<code>`）
   - 图片直接在 Word 里插入 → 自动提取到 `images/posts/`
   - 加粗/斜体自动转 `<strong>/<em>`
4. 保存 Word，**双击 `doc2post.bat`**，输入要转换的文件编号（回车=全部）。
5. 转换成功的 docx 自动移入 `posts/processed/`，避免重复转换。
6. 本地浏览器打开 `posts.html` 预览 → 满意后双击 `deploy.bat` 上线。

### 3.2 发新文章（纯文字骨架方式）

双击 `new_post.bat` → 按提示输入标题/摘要/分类 → 自动在 `main.js` 数组**最前面**插入骨架并自动打开该文件 → 只需补写 content 里的 HTML 正文。适合不含图片的短文章。

### 3.3 手动编辑文章（备用）

直接编辑 `js/main.js` 的 `posts` 数组。字段：`id`（唯一，递增）、`title`、`excerpt`、`date`、`tag`（tech/life/reading）、`tagLabel`（技术/生活/读书）、`content`（反引号包裹的 HTML）。
坑：content 内**不能嵌套反引号**；逗号引号不配对会导致所有文章显示不出来；改完先本地刷新再部署。

### 3.4 部署上线

双击 `deploy.bat`。无改动时会提示无需部署；推送失败时提交仍安全保存在本地，修好网络再跑一次即可。首次推送曾在浏览器弹 GitHub 登录授权（由用户本人完成）。

### 3.5 换首页大图

用任意横版图片覆盖 `E:\Desktop\SterneSite\images\home.jpg`（保持小写文件名），部署即可。

---

## 四、踩坑记录与硬性约定（接手必读）

1. **bat 脚本必须是纯 ASCII ＋ CRLF 换行**。
   - 原因：cmd 读取 .bat 的编码不稳定（GBK/UTF-8 环境差异），中文会变成乱字节破坏脚本结构（曾出现"系统找不到指定的路径"、退出码 255）；LF 换行同样致命。
   - 约定：bat 里一律英文提示；生成方式用 Python 写 `script.replace('\n','\r\n').encode('ascii')`；**不要用 `chcp 65001`**。
2. **bat 里空输出重定向用 `>nul`，绝不能用 `>/dev/null`**（后者是 Linux 写法，Windows 下报"系统找不到指定的路径"）。
3. **生成 Word 模板必须声明东亚字体**：给 Normal/标题样式的 rFonts 设置 `w:eastAsia`（正文宋体、标题黑体），否则 Word 里部分简体字显示为方框。修复后的模板是 `posts/新文章模板.docx`。
4. **`deploy.bat` 会执行 `git add .`**：博客目录下不要遗留临时/调试文件，否则会被一并提交推送（曾误推 `debug_deploy.bat`，已清理）。
5. **网络**：曾出现 github.com:443 连接超时（curl 28），属临时网络/代理问题；push 失败重试即可，本地提交不丢。
6. git 的 `LF will be replaced by CRLF` 警告无害，可忽略。

---

## 五、当前遗留事项（TODO）

- [ ] 用户需**关闭 Word 后手动删除旧模板** `posts/模板-新文章.docx`（此前被 Word 占用，脚本删不掉）。
- [ ] `main.js` 中 **id=7 的文章**《SterneSite Web | 个人主页》是用户用 new_post.py 创建的，正文还是骨架占位文字，需补写或删除。
- [ ] 确认线上网站已呈现最新布局（侧边栏 + 首页大图）。

## 六、用户偏好与环境信息（给接手 Agent）

- 用户是前端初学者（正在学 HTML/CSS/JS），解释要通俗，避免术语堆砌。
- **账号登录、GitHub 授权等敏感操作一律由用户本人在浏览器完成**，不要向用户索要密码/token，也不要代为操作账号。
- 环境：Windows 11；git 2.54；Python 可用且已装 PIL、python-docx；node 存在性未验证（语法检查可试 `node --check`）。
- 用户用 **Word 写作**，这是既定的发文方式，不要劝退到 Markdown。
- 用户曾问过"要不要上后端"，结论是**维持纯静态**（零成本零运维，Cloudflare Pages 自动部署）；如未来文章量上百或需要评论等功能，优先考虑 Cloudflare Workers + D1 类免费方案，而非买服务器。
- 修改网站代码后：先本地验证 → 由用户双击 deploy.bat 部署；不要替用户 push 到远程（除非用户当场明确要求）。

## 七、本次会话完成的工作清单

1. 修复 `about.html` 两处 HTML 标签错误（`/p>` 缺 `<`、多余 `</a>`）。
2. 建立 git 仓库、.gitignore、一键部署 `deploy.bat`（含多次编码/换行/重定向修复）。
3. 全站改版为**侧边栏布局**（三个页面 + 样式重写，含移动端适配）。
4. 首页改为**大图展示**，文章列表迁至新增的 `posts.html`，导航统一为 首页/文章/关于。
5. 生成首页占位图 `images/home.jpg`（星空主题）。
6. 开发 `new_post.py/bat`（一键新建文章骨架）。
7. 开发 `doc2post.py/bat`（Word→文章转换器，支持标题/段落/加粗斜体/代码块/图片提取/元信息解析/自动归档）。
8. 生成并修复 Word 写作模板（字体方框问题）。
